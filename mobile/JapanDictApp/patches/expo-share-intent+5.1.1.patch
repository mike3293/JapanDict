*** Begin Patch
*** Update File: node_modules/expo-share-intent/android/src/main/java/expo/modules/shareintent/ExpoShareIntentModule.kt
@@
-            if (intent.type == null) return
-            if (intent.type!!.startsWith("text/plain")) {
-                // text / urls
-                if (intent.action == Intent.ACTION_SEND) {
-                    notifyShareIntent(mapOf(
-                        "text" to intent.getStringExtra(Intent.EXTRA_TEXT),
-                        "type" to "text",
-                        "meta" to mapOf(
-                            "title" to intent.getCharSequenceExtra(Intent.EXTRA_TITLE),
-                        )
-                    ))
-                } else if (intent.action == Intent.ACTION_VIEW) {
-                    notifyShareIntent(mapOf( "text" to intent.dataString, "type" to "text"))
-                } else {
-                    notifyError("Invalid action for text sharing: " + intent.action)
-                }
+            if (intent.type == null) return
+            if (intent.type!!.startsWith("text/plain")) {
+                // text / urls
+                // Handle ACTION_SEND, ACTION_VIEW and ACTION_PROCESS_TEXT (selected text processor)
+                if (intent.action == Intent.ACTION_SEND || intent.action == "android.intent.action.PROCESS_TEXT") {
+                    // For PROCESS_TEXT the text may be in EXTRA_PROCESS_TEXT (API 23+)
+                    val processText = if (intent.hasExtra("android.intent.extra.PROCESS_TEXT")) {
+                        intent.getStringExtra("android.intent.extra.PROCESS_TEXT")
+                    } else {
+                        intent.getStringExtra(Intent.EXTRA_TEXT)
+                    }
+                    notifyShareIntent(mapOf(
+                        "text" to processText,
+                        "type" to "text",
+                        "meta" to mapOf(
+                            "title" to intent.getCharSequenceExtra(Intent.EXTRA_TITLE),
+                        )
+                    ))
+                } else if (intent.action == Intent.ACTION_VIEW) {
+                    notifyShareIntent(mapOf( "text" to intent.dataString, "type" to "text"))
+                } else {
+                    notifyError("Invalid action for text sharing: " + intent.action)
+                }
*** End Patch
